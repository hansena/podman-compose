#!/bin/bash

# # coerce the path
# cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P

echo "hello: $1"
# ./podman_write_file_to_vm.sh ~/test.conf 'some text'

# ARG1=~/test.conf
# ./podman_write_file_to_vm.sh $ARG1 'some text'
# echo "testing" > $TO
# mkdir -p $1
